{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://macpaw.github.io/portable-memory/schemas/secretRef.schema.json",
  "title": "Portable Memory secret reference",
  "description": "items/secretRef.jsonl — a reference to a vault secret. Carries encryption metadata only; NEVER the plaintext or ciphertext (spec §7).",
  "type": "object",
  "required": ["id", "label", "sensitivity", "category", "preview", "encryptionMetadata", "createdAt"],
  "properties": {
    "id": { "type": "string", "description": "globally unique, stable, kind-prefixed (e.g. sec_…)" },
    "label": { "type": "string" },
    "sensitivity": { "type": "string" },
    "category": { "type": "string" },
    "preview": { "type": "string", "description": "a masked hint only (e.g. last-4) — MUST NOT contain recoverable secret material" },
    "encryptionMetadata": { "type": "string", "description": "how the value is protected — NOT the value" },
    "createdAt": { "type": "string", "format": "date-time" },
    "lastAccessed": { "type": ["string", "null"], "format": "date-time" }
  },
  "additionalProperties": true
}
